/**
 * Executes task code.
 */
class Executor {
    /**
     * Synchronously executes a manifest task.
     *
     * @param {ManifestTask} mfTask - Task definition from a package manifest.
     * @param {Object} parameters - Key/value hash containing job-specific parameters.
     */
    static executeSync(mfTask, parameters) {

    }
}

module.exports = Executor;
